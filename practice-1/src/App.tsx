import "./App.css";

import * as THREE from "three";

function App() {
  // const [count, setCount] = useState(0)
  const scene = new THREE.Scene();
  console.log(scene);

  return <div />;
}

export default App;
